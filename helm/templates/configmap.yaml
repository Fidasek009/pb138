apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "pagepal.fullname" . }}-config
  labels:
    {{- include "pagepal.labels" . | nindent 4 }}
data:
  API_PORT: {{ .Values.api.port | quote }}
